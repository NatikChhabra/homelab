# Copy to .env and fill in. Never commit the real .env.
#
# These must match the credentials the existing Postgres volume was
# initialised with. Changing them does not change the database -- it just
# makes Immich unable to connect to it.
IMMICH_DB_USER=
IMMICH_DB_PASSWORD=
