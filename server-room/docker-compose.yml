services:
  glances:
    image: nicolargo/glances:latest
    restart: always
    pid: host
    ports:
      - "61208:61208"
    environment:
      - "GLANCES_OPT=-w"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro

