# Copy to .env and fill in. Never commit the real .env.
#
# SECRET_ENCRYPTION_KEY decrypts the integration credentials Homarr keeps in
# db.sqlite. Changing it on an existing install makes every stored credential
# unreadable. Generate a fresh one for a new install with:
#   openssl rand -hex 32
SECRET_ENCRYPTION_KEY=
